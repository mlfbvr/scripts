# scripts
A collection of simple scripts I use


## random_wallhaven.sh
Pull a random image from wallhaven.cc and set it as wallpaper. Uses jq to extract the image url and feh to set the wallpaper.
Options are set using ~/.random_wallhaven (see sample for supported options)
